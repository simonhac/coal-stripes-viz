/**
 * Shared types for interaction sessions
 */

export enum SessionType {
  MOVE = 'MOVE',   // For mouse drag movements
  WHEEL = 'WHEEL', // For wheel/trackpad scrolling
  TOUCH = 'TOUCH', // For two-finger touch gestures
}